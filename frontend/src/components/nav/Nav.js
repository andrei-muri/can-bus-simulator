import styles from './Nav.module.css';

const Nav = () => {
    return (
        <div className={styles.container}>
            <h1 className={styles.title}>CAN Bus Simulator</h1>
        </div>
    );
}

export default Nav;